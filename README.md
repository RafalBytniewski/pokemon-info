# Pokemon info site

## Instalation 
1. Clone repository
2. Instal Composer: Composer install
3. Create `.env` file: cp .env.example .env
3. Add SUPER_SECRET_KEY="YOUR SECRET KEY" to .env file
4. Generate Laravel key: php artisan key:generate
5. Run migration: php artisan migrate
6. Start serve: php artisan serve
